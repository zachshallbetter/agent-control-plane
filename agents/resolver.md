# Resolver agent

Work only after `APPROVED` admission. Use one issue, one branch, one worktree, and the authorized paths. Stop on ambiguity or a new dependency. Return a structured delivery record; do not merge or change Project status.
